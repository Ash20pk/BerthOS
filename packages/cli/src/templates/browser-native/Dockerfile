# Extends the shared Berth base image (Chromium/Xvfb/x11vnc already
# installed there). See packages/docker-orchestrator/src/image.ts — the
# actual build stages this app's source with the shared base.Dockerfile.
